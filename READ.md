# JavaScript 기초 실습 🚀

## 📚 학습 내용

### 1. map()
- 배열의 모든 요소를 변환하여 새로운 배열을 만듭니다.
- 예: 포스트 목록에 '인기' 라벨 추가하기

### 2. filter()
- 조건에 맞는 요소만 골라내어 새로운 배열을 만듭니다.
- 예: 특정 작성자(홍길동, 김철수)의 글만 필터링하기

## 🛠 실행 방법
1. `Quokka.js`실행: ctrl + shift + P

### 12강. Vue.js 입문: 반응형 데이터와 기본 디렉티브
 - 가상DOM
 - 수정작업 후 수정된 부분만 적용 -> 메모리 낭비 적은 편
 - 
 - VUE 특징
 - 단일 파일 컴포넌트
 - 객체가 아닌 내가 작업하는 단위만 작게 쪼개어 구현이 가능

 - Vite
 - js 표준 빌드 도구

 - 1. Vite로 프로젝트 생성

 - 2. 반응형 데이터 
 - render -> ref
 - ref : 값이 바뀌는 순간 Vue가 감지하고 화면을 자동으롱 데이트, 단일 값
 - reactive : 객체
 - {{ }} : 데이터를 텍스트로 출력

 - 3. 템플릿 문법 - 바인딩과 이벤트
 - v-if / v-else
 - v-for : key 필수
 - v-model : 타이핑하는 순간 message 변수, 화면이 실시간 변경됨
 - 바인딩 : 
 - 이벤트 :  
 - 
 - v-model : 양방향 데이터 바운딩
### 13강. 13강 Vue 컴포넌트 기초: Props와 Emit
 - 데이터는 위에서 아래로 (props)
 - 이벤트는 아래에서 위로 (Emit)
